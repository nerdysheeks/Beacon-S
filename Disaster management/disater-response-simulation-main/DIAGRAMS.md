# Beacon-S Visual Diagrams

Visual representations of system architecture and data flows.

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        BEACON-S SYSTEM                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────┐              ┌────────────────────┐    │
│  │   Beacon-Chat      │              │   Beacon-Sim       │    │
│  │   (Module 1)       │              │   (Module 2)       │    │
│  │                    │              │                    │    │
│  │  • P2P WebRTC      │              │  • MANET Viz       │    │
│  │  • E2EE (AES-GCM)  │              │  • DTN Simulation  │    │
│  │  • WoT-PoW         │              │  • SHA-256 Check   │    │
│  └────────────────────┘              └────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## WebRTC Connection Flow

```
┌──────────┐                  ┌──────────┐                  ┌──────────┐
│ Client A │                  │  Server  │                  │ Client B │
└────┬─────┘                  └────┬─────┘                  └────┬─────┘
     │                             │                             │
     │ 1. Generate ECDH KeyPair    │                             │
     │ ────────────────────────>   │                             │
     │                             │                             │
     │ 2. join-room("shelter1")    │                             │
     │ ──────────────────────────> │                             │
     │                             │                             │
     │                             │ 3. join-room("shelter1")    │
     │                             │ <────────────────────────── │
     │                             │                             │
     │ 4. existing-peers: [B]      │                             │
     │ <────────────────────────── │                             │
     │                             │                             │
     │ 5. Create RTCPeerConnection │                             │
     │ ────────────────────────>   │                             │
     │                             │                             │
     │ 6. webrtc-offer + publicKey │                             │
     │ ──────────────────────────> │ ──────────────────────────> │
     │                             │                             │
     │                             │ 7. Create RTCPeerConnection │
     │                             │ <────────────────────────── │
     │                             │                             │
     │                             │ 8. webrtc-answer + publicKey│
     │ <────────────────────────── │ <────────────────────────── │
     │                             │                             │
     │ 9. Derive Shared AES Key    │                             │
     │ ────────────────────────>   │                             │
     │                             │                             │
     │                             │ 10. Derive Shared AES Key   │
     │                             │ <────────────────────────── │
     │                             │                             │
     │ 11. ICE Candidates Exchange │                             │
     │ <──────────────────────────────────────────────────────> │
     │                             │                             │
     │ 12. RTCDataChannel OPEN     │                             │
     │ ═══════════════════════════════════════════════════════> │
     │                                                           │
     │ 13. Encrypted Messages (P2P, no server involvement)      │
     │ <═══════════════════════════════════════════════════════> │
     │                                                           │
```

## Encryption Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    MESSAGE ENCRYPTION FLOW                       │
└─────────────────────────────────────────────────────────────────┘

Sender Side:
┌──────────────┐
│ Plain Text   │  "Hello World"
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Generate IV  │  Random 12 bytes
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ AES-GCM Encrypt      │  Using shared key + IV
│ crypto.subtle.encrypt│
└──────┬───────────────┘
       │
       ▼
┌──────────────┐
│ Ciphertext   │  Base64 encoded
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ Send via DataChannel │  { ciphertext, iv }
└──────────────────────┘

Receiver Side:
┌──────────────────────┐
│ Receive from Channel │  { ciphertext, iv }
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ AES-GCM Decrypt      │  Using shared key + IV
│ crypto.subtle.decrypt│
└──────┬───────────────┘
       │
       ▼
┌──────────────┐
│ Plain Text   │  "Hello World"
└──────────────┘
```

## Proof-of-Work Algorithm

```
┌─────────────────────────────────────────────────────────────────┐
│                    PROOF-OF-WORK FLOW                            │
└─────────────────────────────────────────────────────────────────┘

Sender (Unverified User):
┌──────────────────┐
│ User clicks      │
│ "Send SOS"       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Get timestamp    │  timestamp = Date.now()
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ nonce = 0        │
└────────┬─────────┘
         │
         ▼
    ┌────────────────────────────────┐
    │ Loop:                          │
    │   data = message + timestamp   │
    │          + nonce               │
    │   hash = SHA256(data)          │
    │                                │
    │   if hash.startsWith("0000")   │
    │     ✓ Found!                   │
    │   else                         │
    │     nonce++                    │
    │     continue                   │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────┐
    │ Send message with  │
    │ { nonce, timestamp }│
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │ Encrypt & transmit │
    └────────────────────┘

Receiver:
┌──────────────────┐
│ Receive message  │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────┐
│ Extract:                 │
│  - message               │
│  - timestamp             │
│  - nonce                 │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Verify:                  │
│ data = message +         │
│        timestamp + nonce │
│ hash = SHA256(data)      │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ if hash.startsWith("0000")│
│   ✓ Accept message       │
│ else                     │
│   ✗ Reject (spam)        │
└──────────────────────────┘
```

## Trust Level Decision Tree

```
                    ┌─────────────────┐
                    │  User sends SOS │
                    └────────┬────────┘
                             │
                ┌────────────┴────────────┐
                │   What is trust level?  │
                └────────────┬────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐    ┌───────────────┐    ┌──────────────┐
│  UNVERIFIED   │    │    VOUCHED    │    │  RESPONDER   │
└───────┬───────┘    └───────┬───────┘    └──────┬───────┘
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐    ┌───────────────┐    ┌──────────────┐
│ Compute PoW   │    │ Check rate    │    │ Send         │
│ (5-10 sec)    │    │ limit         │    │ immediately  │
└───────┬───────┘    └───────┬───────┘    └──────────────┘
        │                    │
        ▼                    ▼
┌───────────────┐    ┌───────────────┐
│ Send with     │    │ If < 1 min    │
│ proof         │    │ since last:   │
└───────────────┘    │ REJECT        │
                     │ Else: SEND    │
                     └───────────────┘
```

## MANET Visualization

```
┌─────────────────────────────────────────────────────────────────┐
│                    MANET (Mobile Ad-Hoc Network)                 │
└─────────────────────────────────────────────────────────────────┘

Initial State (Nodes Far Apart):
    👥 Survivor-1              🚁 Drone-1              📡 Edge Hub
    
    (No connections - out of range)


After Dragging Nodes Together:
    👥 Survivor-1 ═══════ 🚁 Drone-1 ═══════ 📡 Edge Hub
    
    (Green animated edges appear when distance < 250px)


Mesh Network (All in Range):
         👥 Survivor-1
        ╱  ╲
       ╱    ╲
      ╱      ╲
    🚁 Drone-1 ═══════ 📡 Edge Hub
      ╲      ╱
       ╲    ╱
        ╲  ╱
         👥 Survivor-2
    
    (Full mesh connectivity)
```

## DTN Data Mule Flow

```
┌─────────────────────────────────────────────────────────────────┐
│              DTN (Disruption-Tolerant Network)                   │
│                    Data Mule Pattern                             │
└─────────────────────────────────────────────────────────────────┘

Step 1: Initial Setup
┌──────────────┐                              ┌──────────────┐
│  Survivor    │                              │  Edge Hub    │
│              │                              │              │
│ Messages:    │                              │ Messages: [] │
│  - "Need     │                              │              │
│    water"    │         (Out of range)       │              │
│  - "Medical" │                              │              │
│  - "Help"    │                              │              │
└──────────────┘                              └──────────────┘

Step 2: Drone Collects (Store)
┌──────────────┐         🚁 Drone moves near
│  Survivor    │         ◄─────────────────
│              │
│ Messages: [] │ ──────► 🚁 Drone
│              │         Messages:
│ (Cleared)    │          - "Need water"
│              │          - "Medical"
│              │          - "Help"
│              │         Hash: a3f2b9...
└──────────────┘

Step 3: Drone Delivers (Forward)
                        🚁 Drone moves near
                        ─────────────────►  ┌──────────────┐
                                            │  Edge Hub    │
                        🚁 Drone            │              │
                        Messages: []        │ Messages:    │
                        (Cleared)           │  - "Need     │
                                            │    water"    │
                                            │  - "Medical" │
                                            │  - "Help"    │
                                            │              │
                                            │ ✅ VERIFIED  │
                                            │ Hash matches │
                                            └──────────────┘

Step 4: Tampering Detection
                        🚁 Drone
                        Messages:
                         - "Need water [MODIFIED]"
                         - "Medical"
                         - "Help"
                        Original Hash: a3f2b9...
                        
                        ─────────────────►  ┌──────────────┐
                                            │  Edge Hub    │
                                            │              │
                                            │ ❌ TAMPERED  │
                                            │ Hash mismatch│
                                            └──────────────┘
```

## Data Integrity Verification

```
┌─────────────────────────────────────────────────────────────────┐
│                  SHA-256 INTEGRITY CHECK                         │
└─────────────────────────────────────────────────────────────────┘

Collection Phase:
┌──────────────────────┐
│ Messages:            │
│  ["msg1", "msg2"]    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ SHA-256 Hash         │
│ hash = SHA256(       │
│   JSON.stringify(    │
│     messages         │
│   )                  │
│ )                    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Store:               │
│  messages: [...]     │
│  originalHash: a3f2b9│
└──────────────────────┘

Delivery Phase (No Tampering):
┌──────────────────────┐
│ Received:            │
│  messages: [...]     │
│  originalHash: a3f2b9│
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Recalculate Hash     │
│ currentHash = SHA256(│
│   JSON.stringify(    │
│     messages         │
│   )                  │
│ )                    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Compare:             │
│ currentHash == orig? │
│ a3f2b9 == a3f2b9     │
│ ✅ YES → VERIFIED    │
└──────────────────────┘

Delivery Phase (With Tampering):
┌──────────────────────┐
│ Received:            │
│  messages: [MODIFIED]│
│  originalHash: a3f2b9│
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Recalculate Hash     │
│ currentHash = SHA256(│
│   JSON.stringify(    │
│     messages         │
│   )                  │
│ )                    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Compare:             │
│ currentHash == orig? │
│ 7d8e1c != a3f2b9     │
│ ❌ NO → TAMPERED     │
└──────────────────────┘
```

## Component Hierarchy

```
App.jsx
├── BrowserRouter
│   ├── Navbar
│   │   ├── Logo
│   │   └── Navigation Links
│   │       ├── /chat
│   │       └── /simulator
│   │
│   └── Routes
│       ├── / (Home)
│       │   └── Home Component
│       │       ├── Welcome Message
│       │       └── Module Cards
│       │
│       ├── /chat (Beacon-Chat)
│       │   └── Chat Component
│       │       ├── Join Form
│       │       │   ├── Room Input
│       │       │   └── Join Button
│       │       │
│       │       ├── Trust Selector
│       │       │   └── Dropdown
│       │       │
│       │       ├── Messages Area
│       │       │   └── Message List
│       │       │       ├── Message (me)
│       │       │       └── Message (peer)
│       │       │
│       │       └── Input Area
│       │           ├── Text Input
│       │           ├── Send Button
│       │           └── Send SOS Button
│       │
│       └── /simulator (Beacon-Sim)
│           └── Simulator Component
│               ├── Header
│               │   └── Add Node Buttons
│               │
│               ├── React Flow Canvas
│               │   ├── Nodes
│               │   │   ├── Survivor Nodes
│               │   │   ├── Drone Nodes
│               │   │   └── Edge Hub Nodes
│               │   │
│               │   ├── Edges (Auto-generated)
│               │   │
│               │   └── Controls
│               │       ├── Zoom
│               │       ├── Fit View
│               │       └── Minimap
│               │
│               └── Info Panel
│                   ├── Network Stats
│                   ├── Selected Node Info
│                   ├── Drone Actions
│                   │   ├── Collect Button
│                   │   ├── Deliver Button
│                   │   └── Tamper Button
│                   │
│                   └── Instructions
```

## Network Topology Examples

```
┌─────────────────────────────────────────────────────────────────┐
│                    TOPOLOGY EXAMPLES                             │
└─────────────────────────────────────────────────────────────────┘

1. Star Topology (Hub-and-Spoke):
              📡 Edge Hub
             ╱  │  ╲
            ╱   │   ╲
           ╱    │    ╲
         👥    👥    🚁
    Survivor Survivor Drone

2. Linear Chain:
    👥 ═══ 🚁 ═══ 🚁 ═══ 📡
    Survivor  Drone  Drone  Hub

3. Mesh Network:
         👥 ═══ 👥
         ║  ╲ ╱  ║
         ║   ╳   ║
         ║  ╱ ╲  ║
         🚁 ═══ 📡

4. Isolated Groups:
    👥 ═══ 👥        🚁 ═══ 📡
    (Group A)        (Group B)
    
    (No connection between groups)
```

---

These diagrams provide visual understanding of the Beacon-S system architecture, data flows, and network topologies.
